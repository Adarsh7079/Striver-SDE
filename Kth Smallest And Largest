vector<int> kthSmallLarge(vector<int> &arr, int n, int k)
{
	sort(arr.begin(),arr.end());
    vector<int>ans;
    ans.push_back(arr[k-1]);
    int f=arr.size()-k;
    ans.push_back(arr[f]);
    return ans;
}
