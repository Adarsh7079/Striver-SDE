 // i/p:--2 -3 4 -1 -2 1 5 -3
 //op:- 7
 int sum=0;
    int maxi=INT_MIN;
    for(int i=0;i<n;i++)
    {
        sum+=v[i];
        maxi=max(sum,maxi);
        if(sum<0)
        {
            sum=0;
        }
    }
    return maxi;
