 vector<int> res;
        if(!root)
        {
            return res;
        }
        stack<TreeNode*> st;
        TreeNode*curr=root;
        while(curr||!st.empty())
        {
            while(curr)
            {
                st.push(curr);
                curr=curr->left;
            }
            TreeNode*temp=st.top();
            st.pop();
            res.push_back(temp->val);
            curr=temp->right;

        }
        return res;
